
{% macro edit_modal(editlink ,repertoires ) %}
      <form  enctype="multipart/form-data" method = "POST" action = "{{editlink}}">

<div class="modal fade" id="alterfile" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="exampleModalLabel">Edit File</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>

      <div class="modal-body">
         <input  class = "floating-input form-control"type = "text" placeholder = "Name of new file" name = "altered_name" />
         
 <input  type = "file" placeholder = "Name of new folder" name = "load" />
         <select  name ="alter_parent"class="browser-default custom-select" >
         <option value =null  selected></option>
          {% for folder in repertoires %}
          <option value  = {{folder.id}} > {{folder}}</option>
          {% endfor %}
          </select>
         </div>

 
    <div class="modal-footer">  
        <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
        <button type="submit" class="btn btn-primary">Save changes</button>
          </form>
      </div>
    </div>
  </div>
</div>   

{% endmacro %}
{% macro folder_edit(quickedit , repertoires ) %}
<style>
         input[type=text] {
            width: 100%; 
            /* /* padding: 10px 15px; */
            margin: 5px 0; 
            box-sizing: border-box; */
            background-color: lightgray;
         }
      </style>
 <form  enctype="multipart/form-data" method = "POST" action = "{{quickedit}}">

<div class="modal fade" id="alterfolder" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="exampleModalLabel">Edit Folder</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>

      <div class="modal-body">
       <div class="form-outline col-auto">
    <label class="form-label" for="formTextExample2">Name of new folder</label>
    <input type="text" id="formTextExample2" class="form-control" , aria-describedby="textExample2" name = "foldname"  />
  </div>
         {# <input  class = "floating-input form-control" id = "txt"type = "text" placeholder = "" /> #}
            {# <label class="form-label" for="txt">Name of new folder</label> #}
         <br>
         <label >Select a  parent folder</label>
             <select  name ="altparent"class="browser-default custom-select"  >
         <option value =null  selected> Select a folder</option>
          {% for folder in repertoires %}
          <option value  = {{folder.id}} > {{folder}}</option>
          {% endfor %}
          </select>
         </div>

 
    <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
        <button type="submit" class="btn btn-primary">Save changes</button>
          </form>
      </div>
    </div>
  </div>
</div>     
{% endmacro %}